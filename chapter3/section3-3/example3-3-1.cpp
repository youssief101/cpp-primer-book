#include <iostream>
using namespace std;
#include <string>
#include <vector>

int main() {
    vector<int> ivec;   // ivec holds objects of type int
    vector<string> svec;    // svec hold objects of type string
    vector<vector<string>> file;    // file holds objects of type vector that hold elements type string
    return 0;
}