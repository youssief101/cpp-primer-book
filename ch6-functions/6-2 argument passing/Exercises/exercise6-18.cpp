class matrix;   // forward declaration of the class

bool compare(const matrix &m1, const matrix &m2);

#include <vector>

std::vector<int>::iterator change_val(int value, std::vector<int>::iterator iter);